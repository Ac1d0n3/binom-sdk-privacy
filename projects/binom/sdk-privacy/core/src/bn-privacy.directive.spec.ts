import { BnPrivacyDirective } from './bn-privacy.directive';

describe('BnPrivacyDirective', () => {
  it('should create an instance', () => {
    const directive = new BnPrivacyDirective();
    expect(directive).toBeTruthy();
  });
});
